const randomNumber1 = Math.random();

if (`0 === 0`) {
    // 条件式を満たす場合の処理
    window.location.href = "{index2.html}";
  } else {
    // 条件式を満たさない場合の処理
    window.location.href = "{nansore.jpg}";
  }
  